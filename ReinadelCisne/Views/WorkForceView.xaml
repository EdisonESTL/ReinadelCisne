<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ReinadelCisne.ViewModels"
             x:Class="ReinadelCisne.Views.WorkForceView"
             Title="Mano de Obra">
    <ContentPage.BindingContext>
        <vm:WorkForceVM/>
    </ContentPage.BindingContext>
    <ContentPage.Content>        
        <StackLayout Padding="7">
            <StackLayout Margin="11">
                <Label Text="Tipo de mano de obra"
                           Style="{StaticResource LabelTittles1}"/>
                <StackLayout Orientation="Horizontal"
                         HorizontalOptions="Center"
                         Margin="0,0,0,0">
                    <Button Text="Directo"
                        Style="{StaticResource Button2}"
                                BorderColor="Black"
                                BorderWidth="7"
                                Command="{Binding PushCommand}"
                                CommandParameter="directo"/>
                    <Button Text="Indirecto"
                        Style="{StaticResource Button2}"
                                BorderColor="Black"
                                BorderWidth="7"
                                Command="{Binding PushCommand}"
                                CommandParameter="indirecto"/>
                </StackLayout>
            </StackLayout>
            <Frame Style="{StaticResource Frame4}"
                   Margin="0,0,0,11">
                <StackLayout Orientation="Horizontal"
                         HorizontalOptions="CenterAndExpand"
                         Spacing="30">
                    <StackLayout>
                        <Label x:Name="TotalElementos"
                           Style="{StaticResource LabelTittlesColLectionView}">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="Total elementos&#10;"></Span>
                                    <Span Text="{Binding TotalElementos}"
                                      FontSize="20"></Span>
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>
                    </StackLayout>
                    <StackLayout>
                        <Label x:Name="TotalValor"
                           Style="{StaticResource LabelTittlesColLectionView}">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="Total elementos&#10;"></Span>
                                    <Span Text="{Binding TotalValores, StringFormat=\{0:c\}}"
                                      FontSize="20"></Span>
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>
                    </StackLayout>
                </StackLayout>
            </Frame>
            
            <CollectionView x:Name="ListWF"
                            ItemsSource="{Binding WorkForces}"
                            SelectionMode="Single"
                            SelectionChangedCommand="{Binding SelectdCommand}" 
                            SelectionChangedCommandParameter="{Binding SelectedItem , Source={x:Reference ListWF}}" 
                            Margin="10,0,10,0">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical"
                        ItemSpacing="20"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame CornerRadius="10"
                               BackgroundColor="{StaticResource QuintoColor}">
                            <SwipeView BackgroundColor="Transparent">
                                <SwipeView.RightItems>
                                    <SwipeItems>                                    
                                        <SwipeItem Text="Borrar"
                                           IconImageSource="deleteL.png"
                                           BackgroundColor="LightPink"
                                           Command="{Binding Path=BindingContext.EliminarCommand , Source={x:Reference ListWF}}"
                                           CommandParameter="{Binding .}"/>
                                        <SwipeItem Text="Modificar"
                                           IconImageSource="newa.png"
                                           BackgroundColor="LightGreen"
                                           Command="{Binding Path=BindingContext.ModificarCommand , Source={x:Reference ListWF}}"
                                           CommandParameter="{Binding .}"/>
                                    </SwipeItems>
                                </SwipeView.RightItems>
                                <!-- Content -->
                            
                                <Grid RowDefinitions="Auto, Auto"
                                    ColumnDefinitions="*, 2*"
                                    HorizontalOptions="Center">
                                    <Image Source="manobra.png"
                                       Grid.Row="0"
                                       Grid.Column="0"/>
                                    <Label TextColor="Black"
                                   Grid.Row="0"
                                       Grid.Column="1">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="Puesto: " FontAttributes="Bold"
                                              />
                                                <Span Text="{Binding Name}"
                                                      FontSize="16"/>
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                    <Label TextColor="Black"
                                   Grid.Row="1"
                                       Grid.Column="0">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="Tipo: " FontAttributes="Bold"/>
                                                <Span Text="{Binding Type}"/>
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                    <Label TextColor="Black"
                                   Grid.Row="1"
                                       Grid.Column="1">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="Pago mensual: " FontAttributes="Bold"/>
                                                <Span Text="{Binding PayMonth, StringFormat=\{0:c\}}"
                                                      FontSize="16"/>
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                </Grid>                            
                            </SwipeView>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                <CollectionView.EmptyView>
                    <StackLayout HorizontalOptions="CenterAndExpand"
                             VerticalOptions="CenterAndExpand">
                        <Label Text="Hola, no hay mano de obra registrada para la producción de un producto."
                                   Margin="10,25,10,10"
                                   Style="{StaticResource LabelTittlesColLectionView}"
                                   HorizontalOptions="Fill"
                                   HorizontalTextAlignment="Center" 
                                   TextColor="Black"/>
                    </StackLayout>
                </CollectionView.EmptyView>
            </CollectionView>
            <Grid ColumnDefinitions="Auto, Auto"
                  RowDefinitions="Auto"
                  HorizontalOptions="Center">
                <Button Text="Cancelar"
                        Grid.Column="0"
                        Style="{StaticResource ButtonCancel}"
                        Command="{Binding PushCommand}"
                        CommandParameter="regresar"
                        CornerRadius="30"/>
                <Button Text="Nuevo MO"
                        Grid.Column="1"
                        TextColor="White"
                        Style="{StaticResource Button1}"
                        Command="{Binding PushCommand}"
                        CommandParameter="nuevoM"
                        CornerRadius="30"/>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>