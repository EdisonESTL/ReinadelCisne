<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ReinadelCisne.ViewModels"
             x:Class="ReinadelCisne.Views.RawMaterial"
             Title="Materia Prima">
    <ContentPage.BindingContext>
        <vm:RawMaterialVm/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout Padding="10">
            <Label x:Name="FiltroTitle"
                Text="Filtrar por grupo"
                   TextColor="Black"/>
            <Grid x:Name="Cuerpofiltro"
                ColumnDefinitions="*, Auto, Auto"
                  RowDefinitions="Auto"
                  Margin="60,0,40,20">
                <Picker x:Name="picker"
                        Grid.Column="0"
                    Title="Grupo"
                    TitleColor="Black"
                    BackgroundColor="White"
                    ItemsSource="{Binding GroupsRMs}"
                    ItemDisplayBinding="{Binding Description}"
                    SelectedItem="{Binding GroupSelected}"/>
                <ImageButton Source="searchhd.png"
                             BackgroundColor="{StaticResource PrimerColor}"
                             CornerRadius="35"
                             Grid.Column="1"
                             HorizontalOptions="CenterAndExpand"
                             Command="{Binding PerformSearch}"
                             Aspect="AspectFit"
                             Padding="10,0,10,0"/>
                <ImageButton Source="arbol1.png"                             
                             BackgroundColor="{StaticResource SegundoColor}"
                             CornerRadius="35"
                             Grid.Column="2"
                             HorizontalOptions="CenterAndExpand"
                             Command="{Binding PushCommand}"
                             CommandParameter="todo"
                             Aspect="AspectFit"
                             Padding="15,0,15,0"/>
            </Grid>

            <Frame BorderColor="#AAFDF7"
                   CornerRadius="10"
                   Padding="0,10,0,10"
                   Margin="0,10,0,18"
                   VerticalOptions="Center">
                <Frame.Background>
                    <LinearGradientBrush StartPoint="0,0"
                                         EndPoint="1,1">
                        <GradientStop Color="#AAFDF7"
                                          Offset="0.1" />
                        <GradientStop Color="#FFFFFF"
                                          Offset="1.0" />
                    </LinearGradientBrush>
                </Frame.Background>
                <StackLayout Orientation="Horizontal"
                         HorizontalOptions="Center"
                         Spacing="30"
                             Margin="10">
                    <StackLayout>
                        <Label x:Name="TotalElementos"
                           Style="{StaticResource LabelTittlesColLectionView}">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="Total elementos&#10;"></Span>
                                    <Span Text="{Binding TotalElementos}"
                                      FontSize="19"></Span>
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>
                    </StackLayout>
                    <StackLayout>
                        <Label x:Name="TotalValor"
                           Style="{StaticResource LabelTittlesColLectionView}">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="Total valor&#10;"></Span>
                                    <Span Text="{Binding TotalValores, StringFormat=\{0:c\}}"
                                      FontSize="19"></Span>
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>
                    </StackLayout>
                </StackLayout>
            </Frame>
            
            <RefreshView IsRefreshing="{Binding IsRefreshing}" 
                         Command="{Binding RefreshCommand}"
                         Margin="10,0,10,0"
                         VerticalOptions="CenterAndExpand"
                         HorizontalOptions="CenterAndExpand">
                <CollectionView x:Name="listaMateriaPrima" 
                    ItemsSource="{Binding ListRawMaterialsComplete, Mode=OneWay}" 
                                SelectionMode="Single"                                 
                                SelectionChangedCommand="{Binding SelectdCommand}" 
                                SelectionChangedCommandParameter="{Binding SelectedItem , Source={x:Reference listaMateriaPrima}}" 
                               >
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Vertical" ItemSpacing="7"/>
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame Style="{StaticResource FrameProductss}"
                                   BackgroundColor="{StaticResource QuintoColor}"
                                   HorizontalOptions="Center"
                                   Padding="3">
                                <SwipeView BackgroundColor="Transparent">
                                    <SwipeView.RightItems>
                                        <SwipeItems>
                                            <SwipeItem Text="Eliminar"                                                   
                                                       IconImageSource="deleteL.png"
                                                       BackgroundColor="LightPink"
                                                       Command="{Binding Path=BindingContext.EliminarCommand , Source={x:Reference listaMateriaPrima}}"
                                                       CommandParameter="{Binding .}"/>
                                            <SwipeItem Text="Modificar"
                                                       IconImageSource="newa.png"
                                                       BackgroundColor="LightGreen"
                                                       Command="{Binding Path=BindingContext.ModificarCommand , Source={x:Reference listaMateriaPrima}}"
                                                       CommandParameter="{Binding .}"/>
                                        </SwipeItems>
                                    </SwipeView.RightItems>
                                    <Grid ColumnDefinitions="*, 2*" >
                                        <StackLayout Grid.Column="0" VerticalOptions="Center">
                                            <Frame CornerRadius="100"
                                                   HeightRequest="100"
                                                   WidthRequest="100"
                                           HorizontalOptions="Center"
                                           BackgroundColor="Transparent"                       
                                           Padding="0"
                                               >
                                                <Image x:Name="image"
                                                   Aspect="AspectFill"
                                           Source="{Binding Image.Source}"/>
                                            </Frame>
                                            
                                        </StackLayout>                                        

                                        <StackLayout Grid.Column="1">
                                            <Label                                          
                                               Style="{StaticResource LabelRegistroVenta}" 
                                               FontSize="20">
                                                <Label.FormattedText>
                                                    <FormattedString>
                                                        <Span Text="{Binding Raw.NameRM}"/>
                                                    </FormattedString>
                                                </Label.FormattedText>
                                            </Label>
                                            <Label 
                                               Text="{Binding Raw.DescriptionRM}" Style="{StaticResource LabelTittles1}" FontSize="15"/>                                            
                                            <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
                                                <Label                                            
                                               Style="{StaticResource LabelRegistroVenta}" 
                                               FontSize="15">
                                                    <Label.FormattedText>
                                                        <FormattedString>
                                                            <Span Text="Cant. "/>
                                                            <Span Text="{Binding Raw.CantidadRM}" />
                                                        </FormattedString>
                                                    </Label.FormattedText>
                                                </Label>

                                                <Label VerticalOptions="Center"                                     
                                               Style="{StaticResource LabelRegistroVenta}" 
                                               FontSize="15">
                                                    <Label.FormattedText>
                                                        <FormattedString>
                                                            <Span Text="{Binding Raw.UMedidaRM.Description}"
                                                          TextColor="Black"/>
                                                        </FormattedString>
                                                    </Label.FormattedText>
                                                </Label>
                                            </StackLayout>
                                            
                                            <Label                                          
                                               Style="{StaticResource LabelRegistroVenta}" 
                                               FontSize="15">
                                                <Label.FormattedText>
                                                    <FormattedString>
                                                        <Span Text="V/U "/>
                                                        <Span Text="{Binding Raw.CostoRM, StringFormat=\{0:C\}}"  />
                                                    </FormattedString>
                                                </Label.FormattedText>
                                            </Label>
                                        </StackLayout>
                                    </Grid>
                                </SwipeView>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                    <CollectionView.EmptyView>
                        <StackLayout HorizontalOptions="CenterAndExpand"
                             VerticalOptions="CenterAndExpand">
                            <Label Text="Hola, no hay materia prima registrada."
                                   Margin="10,25,10,10"
                                   Style="{StaticResource LabelTittlesColLectionView}"
                                   HorizontalOptions="Fill"
                                   HorizontalTextAlignment="Center" 
                                   TextColor="Black"/>
                        </StackLayout>
                    </CollectionView.EmptyView>
                </CollectionView>
            </RefreshView>
            <Grid RowDefinitions="Auto" Margin="10">
                <!--<Button Text="Inicio" Command="{Binding PushCommand}" 
                        Style="{StaticResource Button2}"
                        CommandParameter="inicio"
                        Grid.Column="0"
                        FontAttributes="Bold"
                        ImageSource="arbol1.png"/>-->
                <Button Text="Compras" Command="{Binding PushCommand}" 
                        Style="{StaticResource Button2}"
                        CommandParameter="compras"
                        Grid.Column="0"
                        FontAttributes="Bold"
                        ImageSource="shopsld.png"
                        ContentLayout="top, 3"/>
                <Button Text="Nuevo" Command="{Binding PushCommand}" 
                        Style="{StaticResource Button1}"
                        CommandParameter="nuevo"
                        CornerRadius="30"
                        Grid.Column="1"
                        ImageSource="newa.png"
                        ContentLayout="top, 3"/>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>